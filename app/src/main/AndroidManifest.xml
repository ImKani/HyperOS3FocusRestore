<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:theme="@android:style/Theme.Material.Light.NoActionBar" android:label="Focus Restore LSPosed" android:allowBackup="false">
        <meta-data android:name="xposedmodule" android:value="true" />
        <meta-data android:name="xposeddescription" android:value="Restore HyperOS focus notification path with diagnostics" />
        <meta-data android:name="xposedminversion" android:value="93" />
    </application>
</manifest>
