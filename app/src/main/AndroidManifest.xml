<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:theme="@android:style/Theme.Material.Light.NoActionBar" android:label="HyperOS3FocusRestore" android:icon="@drawable/ic_hyperos3_focusrestore" android:allowBackup="false">
        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="焦点通知设置">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.INFO" />
            </intent-filter>
        </activity>
        <provider
            android:name=".SettingsProvider"
            android:authorities="com.hyperos3.focusrestore.settings"
            android:exported="true"
            android:grantUriPermissions="false" />
        <meta-data android:name="xposedmodule" android:value="true" />
        <meta-data android:name="xposedscope" android:resource="@array/xposed_scope" />
        <meta-data android:name="xposeddescription" android:value="恢复 HyperOS 3 的焦点通知状态栏显示" />
        <meta-data android:name="xposedminversion" android:value="93" />
        <meta-data android:name="xposedsharedprefs" android:value="true" />
    </application>
</manifest>
